"""Legacy enhanced-traffic demo removed.

The minimalist v2 backend no longer supports the predictive traffic services
referenced by this script. Use `src/main.py` plus the core services under
`services/traffic_service.py` instead.
"""